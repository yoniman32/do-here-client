<maps:mapView
    latitude="{{ latitude }}"
    longitude="{{ longitude }}"
    zoom="{{ zoom }}"
    mapReady="{{ onMapReady }}"
/>
<Page
    xmlns="http://schemas.nativescript.org/tns.xsd"
    xmlns:maps="nativescript-google-maps-sdk"
>
</Page>